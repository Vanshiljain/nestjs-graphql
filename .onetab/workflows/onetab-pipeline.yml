xdsxxsname: Nihar APs
on:x
  push:
    branches
      - mains
      
jobs: c
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Dependencies
      run: | 
        npm install

  

    - name: Build 
      run: |
        npm run build

